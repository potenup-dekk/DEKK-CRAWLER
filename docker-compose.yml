version: "3.8"

services:
  integrated-crawler:
    build: .
    container_name: integrated-crawler-worker
    restart: always
    env_file:
      - .env
    volumes:
      - /home/ec2-user/crawler/data:/app/data
      - /home/ec2-user/crawler/logs:/app/logs
