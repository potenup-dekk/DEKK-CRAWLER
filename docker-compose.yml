version: "3.8"

services:
  integrated-crawler:
    build: .
    container_name: integrated-crawler-worker
    restart: always
    env_file:
      - .env
    volumes:
      - /opt/crawler/data:/app/data
      - /opt/crawler/logs:/app/logs
